class bool
end

class range
    var super = object
    var low = 0
    var high = 0

    fun init(low, high)
        this.low = low
        this.high = high
    end

    fun iter(low, high, block)
        block(low)
        block(high)
    end
end

range
